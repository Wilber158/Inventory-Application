//data validation file that will contain 
//functions that will validate user entered data and make sure 
//it is ready to be used in the backend modules



//validate user input for creating a new part
//empty fields will be handled by the frontend
//this will validate the actual data
//part_number, description, creation_date, priority_flag, part_notes, part_abbreviation, part_prefix, quantity_sold
const validatePart = (part_number, description, creation_date, priority_flag, part_notes, part_abbreviation, part_prefix, quantity_sold) => {
    let errors = [];
    if()

}